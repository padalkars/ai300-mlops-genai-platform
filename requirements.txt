# Core Azure ML and Microsoft Foundry SDKs
# NOTE: The legacy azureml SDK is not compatible with the current Python/Conda setup used here.
# Keep the project on the modern Azure AI stack unless a separate Python 3.10/3.11 environment
# is created specifically for the older azureml-based training script.
azure-ai-ml>=1.30,<2
azure-ai-projects>=2.0,<3
azure-ai-evaluation>=1.15,<2
azure-identity>=1.19,<2
openai>=2.8,<4

# Azure data, secrets, and search
azure-storage-blob>=12.24,<13
azure-keyvault-secrets>=4.9,<5
azure-search-documents>=11.6,<12
azure-cosmos>=4.9,<5

# Azure observability and MLOps tracking
azure-monitor-opentelemetry>=1.6,<2
mlflow>=2.19,<4

# Prompt flow and evaluation workflows
promptflow>=1.18,<2
promptflow-tools>=1.6,<2

# Local training and tabular data
pandas>=2.2,<3
scikit-learn>=1.5,<2
joblib>=1.4,<2
